{
  "version": "2.0.0",
  "name": "bonustime",
  "devDependencies": {
    "babel-core": "^6.26.0",
    "node-sass": "^4.8.3"
  },
  "engines": {
    "node": ">=6.0.0"
  },
  "dependencies": {
    "babel-preset-env": "^1.6.1"
  },
  "scripts": {
    "build": "parcel build --public-url ./ app/index.html && parcel build --public-url ./ app/config.html"
  }
}
